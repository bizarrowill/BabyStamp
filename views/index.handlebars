<h1>Todos!</h1>

<h2>Todos that are not sleepy!</h2>

<ul>
  {{#each todos}}
    {{#unless sleepy}}
      {{> todos/todo-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Todos that are sleepy!</h2>

<ul>
  {{#each todos}}
    {{#if sleepy}}
      {{> todos/todo-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Todo</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Todo Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Sleepy?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div>

  <button type="submit">Add Todo</button>
</form>
